Save context from this conversation to persistent storage. Execute these steps in order:

1. Update `_memory.md` for each project touched — update sections in-place using Edit (never Write). If a `<!-- extended -->` marker is present, only edit content above it. If missing, create from template. If an architectural decision was made, prepend an entry to `personal/projects/second-brain-setup/decisions.md`.

2. If new profile facts or behavioral observations exist, update `_self/about.md` — only add a bullet if the observation isn't already captured. If a behavioral correction or feedback rule was given, update `_self/rules.md` instead — do not write to workspace-scoped memory.

3. Briefly confirm what was written.
