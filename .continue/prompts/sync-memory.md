---
name: sync-memory
description: Save context from this conversation to project memory files
---

Save context from this conversation to persistent storage.

Current state of memory files:

**Project memory:**
{{{ ./personal/projects/second-brain-setup/_memory.md }}}

**Profile:**
{{{ ./_self/about.md }}}

Execute these steps in order:

1. Update `personal/projects/second-brain-setup/_memory.md` — update sections in-place using file editing tools (never full rewrite). If a `<!-- extended -->` marker is present, only edit content above it.

2. If new profile facts or behavioral observations exist, update `_self/about.md` — only add a bullet if the observation is not already captured. Only edit content above `<!-- extended -->`.

3. Briefly confirm what was written.
